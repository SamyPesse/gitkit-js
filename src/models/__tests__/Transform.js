import Repository from '../Repository';
import Transform from '../Transform';

/*
describe('then', () => {
    test('it should call the first callback with a transform', done => {
        const tr = new Transform(new Repository());

        tr.then(_tr => {
            expect(_tr).toBe(tr);
            done();
        });
    });
});

describe('writeFile', () => {
    test('it should return a transform', done => {
        const tr = new Transform(new Repository());

        tr.writeFile('test.md', 'Hello world').then(_tr => {
            expect(_tr).toBe(tr);
            done();
        });
    });
});
*/

test('wait for test', () => {});
